{
  "tool": "metabase",
  "artifacts": [
    {
      "id": "executive_overview",
      "kind": "dashboard",
      "consumes": [
        {
          "node": "customers",
          "columns": ["customer_id", "customer_name", "first_ordered_at", "count_lifetime_orders", "lifetime_spend"]
        },
        {
          "node": "orders",
          "columns": ["order_id", "customer_id", "ordered_at", "order_total"]
        }
      ]
    },
    {
      "id": "ops_open_orders",
      "kind": "question",
      "consumes": [
        {
          "node": "orders",
          "columns": ["order_id", "customer_id", "ordered_at"]
        }
      ]
    }
  ]
}
